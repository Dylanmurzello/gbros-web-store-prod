// main page that loads the storefront - this is where the journey begins
// keeping it clean with just the layout component

import StorefrontLayout from '@/components/StorefrontLayout'

export default function Home() {
  return <StorefrontLayout />
}
